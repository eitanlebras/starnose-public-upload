[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "starnose"
version = "0.1.1"
description = "See what Claude Code is doing"
requires-python = ">=3.8"

[project.scripts]
starnose = "starnose.cli:main"
snose = "starnose.cli:main"
